import { RouteLoading } from "@/components/route-loading";

export default function CheckInResultLoading() {
  return (
    <RouteLoading
      eyebrow="Decoding"
      title="Reading the signal."
      description="Checking the mood, scanning for risk, and shaping one concise reflection with a next step."
    />
  );
}
