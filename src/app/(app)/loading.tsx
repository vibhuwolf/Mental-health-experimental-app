import { RouteLoading } from "@/components/route-loading";

export default function AppLoading() {
  return (
    <RouteLoading
      eyebrow="Opening"
      title="Opening your private space."
      description="MOODDROP is getting the next screen ready with supportive reflection, not diagnosis or emergency support."
    />
  );
}
