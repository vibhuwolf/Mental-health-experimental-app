import { RouteLoading } from "@/components/route-loading";

export default function ReplayLoading() {
  return (
    <RouteLoading
      eyebrow="Mood Replay Studio"
      title="Building your weekly replay."
      description="Pulling together the week’s signal, what helped most, and the therapy-prep bullets without exposing your raw notes."
    />
  );
}
